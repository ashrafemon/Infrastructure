# =============================================================================
# Managed by Ansible — do not edit manually
# =============================================================================
# Generated: {{ ansible_date_time.iso8601 }}

PRODUCTION={{ env_overrides.PRODUCTION | default('false') }}
DB_BIND_ADDRESS={{ env_overrides.DB_BIND_ADDRESS | default('') }}

MYSQL_ROOT_PASSWORD={{ vault_mysql_root_password }}
POSTGRES_PASSWORD={{ vault_postgres_password }}
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD={{ vault_mongo_password }}
REDIS_PASSWORD={{ vault_redis_password }}

PMA_HOST=mysql
PMA_PORT=3306
UPLOAD_LIMIT=256M
PGADMIN_DEFAULT_EMAIL=admin@{{ infra_domain }}
PGADMIN_DEFAULT_PASSWORD={{ vault_pgadmin_password }}
RI_APP_PORT=5540

RABBITMQ_USER=admin
RABBITMQ_PASSWORD={{ vault_rabbitmq_password }}

MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD={{ vault_minio_password }}

GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD={{ vault_grafana_password }}

POSTFIX_HOSTNAME=mail.{{ infra_domain }}
POSTFIX_DOMAIN={{ infra_domain }}
POSTFIX_RELAYHOST={{ postfix_relayhost | default('') }}
POSTFIX_SMTP_USER={{ postfix_smtp_user | default('') }}
POSTFIX_SMTP_PASSWORD={{ vault_postfix_smtp_password | default('') }}
POSTFIX_TLS=yes
POSTFIX_MESSAGE_SIZE=52428800

CROWDSEC_ENROLL_KEY={{ vault_crowdsec_enroll_key | default('') }}
CROWDSEC_AGENT_USER=admin
CROWDSEC_AGENT_PASSWORD={{ vault_crowdsec_password }}

CLOUDFLARE_EMAIL={{ cloudflare_email | default('') }}
CLOUDFLARE_API_TOKEN={{ vault_cloudflare_api_token | default('') }}

COMPOSE_PROJECT_NAME={{ project_name }}
TZ={{ timezone }}

BACKUP_DIR={{ backup_dir }}
BACKUP_RETENTION_DAYS={{ backup_retention_days }}
