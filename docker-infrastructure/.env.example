# =============================================================================
# Docker Infrastructure - Environment Configuration
# =============================================================================
# Copy this file to .env and replace all passwords before deploying.
#   cp .env.example .env
#   openssl rand -base64 32   # use this to generate each password
# =============================================================================

# ---------------------------------------------------------------------------
# Environment Mode
# ---------------------------------------------------------------------------
# PRODUCTION=true  → databases bind to 127.0.0.1, backend network isolated
# PRODUCTION=false → databases bind to 0.0.0.0 (host accessible for dev)
PRODUCTION=false

# Database bind address (affects mysql, postgres, mongo, redis, rabbitmq, minio)
#   Empty (dev):  0.0.0.0 — reachable from host applications
#   127.0.0.1: (prod)  — localhost only, secure
DB_BIND_ADDRESS=

# ---------------------------------------------------------------------------
# Database Passwords
# ---------------------------------------------------------------------------
MYSQL_ROOT_PASSWORD=Ch@ngeMe_R00t!
POSTGRES_PASSWORD=Ch@ngeMe_DB!
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=Ch@ngeMe_R00t!
REDIS_PASSWORD=Ch@ngeMe_Redis!

# Replication
REPLICATION_USER=replicator
REPLICATION_PASSWORD=Ch@ngeMe_Repl!

# ---------------------------------------------------------------------------
# Database Management
# ---------------------------------------------------------------------------
PMA_HOST=mysql
PMA_PORT=3306
UPLOAD_LIMIT=256M
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=Ch@ngeMe_PgAdmin!
RI_APP_PORT=5540

# ---------------------------------------------------------------------------
# RabbitMQ
# ---------------------------------------------------------------------------
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=Ch@ngeMe_Rabbit!

# ---------------------------------------------------------------------------
# MinIO
# ---------------------------------------------------------------------------
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=Ch@ngeMe_MinIO!

# ---------------------------------------------------------------------------
# Grafana
# ---------------------------------------------------------------------------
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=Ch@ngeMe_Grafana!

# ---------------------------------------------------------------------------
# Postfix (profile: production)
# ---------------------------------------------------------------------------
POSTFIX_HOSTNAME=mail.example.com
POSTFIX_DOMAIN=example.com
POSTFIX_RELAYHOST=
POSTFIX_SMTP_USER=
POSTFIX_SMTP_PASSWORD=
POSTFIX_TLS=yes
POSTFIX_MESSAGE_SIZE=52428800

# ---------------------------------------------------------------------------
# CrowdSec (profile: production)
# ---------------------------------------------------------------------------
CROWDSEC_ENROLL_KEY=
CROWDSEC_AGENT_USER=admin
CROWDSEC_AGENT_PASSWORD=Ch@ngeMe_CrowdSec!

# ---------------------------------------------------------------------------
# Arcane (profile: production)
# ---------------------------------------------------------------------------
ARCANE_JWT_SECRET=Ch@ngeMe_Arcane_JWT!

# ---------------------------------------------------------------------------
# Cloudflare (for NPM SSL)
# ---------------------------------------------------------------------------
CLOUDFLARE_EMAIL=admin@example.com
CLOUDFLARE_API_TOKEN=

# ---------------------------------------------------------------------------
# General
# ---------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=infrastructure
TZ=UTC

# ---------------------------------------------------------------------------
# Backup
# ---------------------------------------------------------------------------
BACKUP_DIR=./backups
BACKUP_RETENTION_DAYS=7
