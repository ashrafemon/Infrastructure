primary_conninfo = 'host=postgres port=5432 user=replicator password=REPLICATION_PASSWORD application_name=postgres-replica'
primary_slot_name = replica_slot
hot_standby = on
wal_level = replica
max_wal_senders = 5
wal_keep_size = 1024
